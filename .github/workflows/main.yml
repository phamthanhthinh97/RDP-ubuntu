name: Docker-Ubuntu-Desktop-Fixed

on:
  workflow_dispatch:

jobs:
  run-desktop-container:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Pre-configure Firefox Proxy Profile
        run: |
          # Tạo sẵn cấu hình ép Firefox chạy qua SOCKS5 Proxy của WARP
          mkdir -p /tmp/firefox-profile
          cat << 'EOF' > /tmp/firefox-profile/user.js
          user_pref("network.proxy.type", 1);
          user_pref("network.proxy.socks", "172.17.0.1");
          user_pref("network.proxy.socks_port", 40000);
          user_pref("network.proxy.socks_version", 5);
          user_pref("network.proxy.socks_remote_dns", true);
          EOF

      - name: Start Ubuntu Desktop Docker Container
        run: |
          # Mount file user.js vào thư mục profile mặc định của người dùng root/ubuntu trong container
          # Đồng thời giữ cấu hình --shm-size=2g để Firefox load video mượt mà
          docker run -d \
            --name ubuntu-desktop \
            --restart always \
            -p 80:80 \
            -p 5900:5900 \
            --shm-size=2g \
            -v /tmp/firefox-profile/user.js:/root/.mozilla/firefox/desktop.default/user.js \
            -v /tmp/firefox-profile/user.js:/home/ubuntu/.mozilla/firefox/desktop.default/user.js \
            dorowu/ubuntu-desktop-lxde-vnc

          sleep 5
          docker ps

      - name: Install and Connect Cloudflare WARP (Proxy Mode)
        run: |
          # 1. Cài đặt kho và gói Cloudflare WARP
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update && sudo apt-get install cloudflare-warp -y
          
          # 2. Khởi động dịch vụ hệ thống
          sudo systemctl enable warp-svc
          sudo systemctl start warp-svc
          sleep 3
          
          # 3. Đăng ký và kết nối mạng WARP ở chế độ Proxy
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode proxy
          warp-cli --accept-tos connect
          sleep 5
          
          # 4. Kiểm tra hoạt động của Proxy WARP
          echo "Kiểm tra IP thông qua Proxy WARP (Cloudflare):"
          curl -x socks5://127.0.0.1:40000 -s ifconfig.me || echo "Chưa kết nối được proxy"
          echo ""

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Establish Tailscale Connection
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-ubuntu-docker-$GITHUB_RUN_ID
          
          TS_IP=""
          RETRIES=0
          while [ -z "$TS_IP" ] && [ $RETRIES -lt 10 ]; do
              TS_IP=$(tailscale ip -4)
              sleep 5
              RETRIES=$((RETRIES+1))
          done
          
          if [ -z "$TS_IP" ]; then
              echo "Không lấy được IP Tailscale. Thoát."
              exit 1
          fi
          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV
      
      - name: Verify Web GUI Accessibility
        run: |
          nc -zv localhost 80 || exit 1
          echo "Kết nối nội bộ đến cổng Web GUI thành công!"

      - name: Maintain Connection & Output Link
        run: |
          echo -e "\n=== UBUNTU DESKTOP DOCKER ACCESS ==="
          echo "Cách 1: Mở trình duyệt và truy cập đường dẫn sau:"
          echo "👉 http://$TAILSCALE_IP"
          echo ""
          echo "Cách 2: Sử dụng phần mềm VNC Viewer kết nối tới:"
          echo "👉 $TAILSCALE_IP:5900"
          echo "=====================================\n"
          
          while true; do
              echo "[$(date)] Toàn bộ hệ thống chạy mượt mà: Tailscale ổn định + Firefox chạy qua WARP Proxy!"
              sleep 300
          done
